# Fixed requirements for Telegram bot deployment
# Ensure exact version compatibility

# Core Telegram Bot Library - EXACT version to avoid compatibility issues
python-telegram-bot==20.7

# Environment variables
python-dotenv==1.0.0

# HTTP requests library
requests==2.31.0

# Date handling
python-dateutil==2.8.2

# For deployment platforms like Render
gunicorn==21.2.0

# Optional: If you're also deploying the web components
Flask==3.0.0
Flask-CORS==4.0.0

# Keep these versions consistent to avoid conflicts
certifi>=2023.7.22
urllib3>=1.26.0,<2.0.0
